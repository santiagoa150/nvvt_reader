/**
 * Constants related to campaign HTTP routes or operations.
 * @enum
 * @type {string}
 */
export enum CampaignHttpConstants {
    /**
     * Prefix used in campaign-related HTTP routes.
     */
    PREFIX = 'campaign',
    /**
     * Tag representing campaign-related operations.
     */
    TAG = 'Campaign',
    /**
     * URL for getting paginated campaigns.
     */
    URL_GET_PAGINATED = 'all',
}