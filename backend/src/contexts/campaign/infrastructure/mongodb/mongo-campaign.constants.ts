/**
 * Constants related to MongoDB for the Campaign entity.
 * @enum {string}
 */
export enum MongoCampaignConstants {
    /**
     * Name of the MongoDB collection storing Campaign documents.
     */
    COLLECTION_NAME = 'campaign',
    /**
     * Identifier for the Campaign document within MongoDB.
     */
    DOCUMENT = 'CAMPAIGN_DOCUMENT',
}