/**
 * Constants related to the database connection.
 * @enum
 * @type {string}
 */
export enum DatabaseConstants {
    /**
     * Name of the database connection.
     */
    DATABASE_CONNECTION_NAME = 'DATABASE_CONNECTION_NAME'
}