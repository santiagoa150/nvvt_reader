/**
 * Enum containing shared exception constants used across the application.
 * @enum
 * @type {string}
 */
export enum SharedExceptionConstants {
    /**
     * Constant representing an internal server error.
     */
    INTERNAL_SERVER_ERROR = 'INTERNAL_SERVER_ERROR'
}